const person = {
    name: "Ion Creanga",
    age: 12,
    isStudent: true
};

console.log(person);

console.log(person.name);
console.log(person.age);
console.log(person.isStudent);

person.age = 33;

console.log(person);